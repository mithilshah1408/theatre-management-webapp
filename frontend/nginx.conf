server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # Send all routes to index.html so React Router handles them.
    # Without this, refreshing /screenings returns a 404 from Nginx.
    location / {
        try_files $uri $uri/ /index.html;
    }
}